class Aaaaaaa constructor(passedString: String) {
    private val string: String
    init {
        string = passedString
    }
    /*fun removeVowels(): String {
        var counter: Int = 0
        var convert: Array<Char> = string.toCharArray().toTypedArray()
        for(x in 0..string.length) {
            if((convert[x] == 'a') || (convert[x] == 'e') || (convert[x] == 'i') || (convert[x] == 'o') || (convert[x] == 'u')) {

            } else {
                convertedString[counter] = convert[x]
                counter++
            }
        }
        val newString: String = String(convertedString.toCharArray())
        return newString
    } */
}